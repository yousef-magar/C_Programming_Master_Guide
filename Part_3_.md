
# 🚀 **Part 3 – Mastering Loops + Arrays in C (Level Pro)**

(بالإنجليزي + العربي في نفس الوقت)

## ===============================

# 🔥 القسم الأول: Loops

## ===============================

الـ **loops** في C هي القلب بتاع أي برنامج dynamic…
من غيرها البرنامج بتاعك بيبقى static بيعمل حاجة واحدة ويقفل.

## 💠 1. while Loop

🔹 بتشتغل طول ما الشرط TRUE
🔹 لو الشرط غلط → مش هتشتغل ولا مرة

### ✔ مثال محترف:

```c
#include <stdio.h>

int main() {
    int pin = 0;

    printf("Enter PIN (1234 to unlock): ");
    scanf("%d", &pin);

    while (pin != 1234) {
        printf("Wrong PIN! Try again: ");
        scanf("%d", &pin);
    }

    printf("Unlocked Successfully!\n");

    return 0;
}
```

🎯 **شرح**

* اللوب هنا بتفضل شغالة لحد ما المستخدم يدخل 1234
* أي رقم غلط → يرجع يكرر
* perfect use-case للتأكد من input

---

## 💠 2. do–while Loop

🔹 بتشتغل مرة واحدة *حتى لو الشرط غلط*
🔹 بعد أول تنفيذ تبدأ تفحص الشرط

### ✔ مثال جامد:

```c
int number;

do {
    printf("Enter a positive number: ");
    scanf("%d", &number);
} while (number <= 0);

printf("You entered: %d\n", number);
```

👑 **ليه مفيدة؟**
لما تكون عايز تاخد input على الأقل مرة واحدة.

---

## 💠 3. for Loop

دي أشهر لوب في C
الـ structure بتاعها:

```c
for (initialization; condition; increment) {
    // code
}
```

### ✔ مثال احترافي:

```c
for (int i = 10; i >= 1; i--) {
    printf("%d\n", i);
}
```

💥 تعمل countdown من 10 → 1

---

## 💠 4. break & continue

### ✔ break

تطلع برا اللوب تمامًا.

```c
for (int i = 1; i <= 10; i++) {
    if (i == 5) break;
    printf("%d\n", i);
}
```

👑 output:
1 2 3 4

---

### ✔ continue

تسكِب iteration واحدة وتكمل بعدها.

```c
for (int i = 1; i <= 10; i++) {
    if (i % 2 == 0) continue;
    printf("%d\n", i);
}
```

👑 Output:
1 3 5 7 9
(بيطبع الفردي فقط)

---

## 💠 5. Nested Loops

لوب جوة لوب.

### ✔ مثال خرافي:

طباعة مربع نجوم 5×5

```c
for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 5; j++) {
        printf("* ");
    }
    printf("\n");
}
```

👑 شكل الناتج:

```
* * * * *
* * * * *
* * * * *
* * * * *
* * * * *
```

---

# ===============================

# 🔥 القسم الثاني: Arrays

# ===============================

الـ **array** ببساطة:
متغير واحد بيشيل مجموعة عناصر من نفس النوع.

## 💠 1. تعريف Array

```c
int numbers[5] = {10, 20, 30, 40, 50};
```

* size = 5
* index من 0 → 4

---

## 💠 2. الوصول للعناصر

```c
printf("%d\n", numbers[0]); // 10
printf("%d\n", numbers[4]); // 50
```

---

## 💠 3. تغيير قيمة

```c
numbers[2] = 999;
```

---

## 💠 4. حساب حجم Array

### ✔ المعادلة الأسطورية:

```c
int size = sizeof(numbers) / sizeof(numbers[0]);
```

---

## 💠 5. Loop على Array

```c
for (int i = 0; i < size; i++) {
    printf("%d ", numbers[i]);
}
```

---

## 💠 6. Array initialized by User Input

### ✔ مثال:

```c
int scores[5];

for (int i = 0; i < 5; i++) {
    printf("Enter score %d: ", i+1);
    scanf("%d", &scores[i]);
}
```

---

# ===============================

# 🔥 القسم الثالث: 2D Arrays

# ===============================

عبارة عن **array of arrays**
شكلها كده:

```c
int matrix[2][3] = {
    {1, 2, 3},
    {4, 5, 6}
};
```

### ✔ الطباعة:

```c
for (int i = 0; i < 2; i++) {
    for (int j = 0; j < 3; j++) {
        printf("%d ", matrix[i][j]);
    }
    printf("\n");
}
```

---

# ===============================

# 🔥 القسم الرابع: Arrays of Strings

# ===============================

### ✔ مثال خرافي:

```c
char names[3][20];

for (int i = 0; i < 3; i++) {
    printf("Enter name %d: ", i+1);
    scanf(" %19[^\n]", names[i]);
}

printf("\n--- Names List ---\n");
for (int i = 0; i < 3; i++) {
    printf("%s\n", names[i]);
}
```

---

# ===============================

# 🔥 القسم الخامس: Ternary Operator

# ===============================

شورت كود للـ if–else

```c
int a = 10, b = 20;

int max = (a > b) ? a : b;

printf("Max = %d", max);
```

---

# ===============================

# 🔥 القسم السادس: Typeof

# ===============================

ممكن تعمل alias سريع لنوع معين:

```c
typeof(int) number;
typeof(char[50]) text;
```

---

# 🎁 BONUS: تمرين جامد + الحل

## ✔ تمرين:

اكتب برنامج

* ياخد 5 أرقام
* يطبع أكبر رقم
* وأصغر رقم
* ومتوسط الأرقام

## ✔ الحل:

```c
#include <stdio.h>

int main() {
    int nums[5];
    int max, min;
    float sum = 0;

    for (int i = 0; i < 5; i++) {
        printf("Enter number %d: ", i+1);
        scanf("%d", &nums[i]);
        sum += nums[i];
    }

    max = min = nums[0];

    for (int i = 1; i < 5; i++) {
        if (nums[i] > max) max = nums[i];
        if (nums[i] < min) min = nums[i];
    }

    printf("Max = %d\n", max);
    printf("Min = %d\n", min);
    printf("Average = %.2f\n", sum / 5);

    return 0;
}
```

---
